using System;

class Programa {
    static void Main(string[] args) {
        int[] numeros = { 1, 2, 3, 4, 5 };

        Console.WriteLine("El contenido del arreglo es este:");
        for (int i = 0; i < numeros.Length; i++) {
            Console.WriteLine(numeros[i]);
        }
    }
}
